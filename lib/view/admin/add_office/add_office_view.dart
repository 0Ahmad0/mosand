import 'package:flutter/material.dart';

import 'widgets/add_office_view_body.dart';

class AddOfficeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AddOfficeViewBody(),
    );
  }
}
